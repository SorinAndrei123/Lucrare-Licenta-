<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplicatielicenta">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".logare.CheckUser"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicatieLicenta"
        android:usesCleartextTraffic="true">
        <activity android:name=".profesor.DashboardQuizActivity"></activity>
        <activity android:name=".profesor.CreareQuizActivity" />
        <activity android:name=".general.AlegereContactActivity" />
        <activity android:name=".profesor.CreareTaskProfesorActivity" />
        <activity android:name=".profesor.ContPersonalProfesorActivity" />
        <activity android:name=".student.ContPersonalActivity" />
        <activity android:name=".logare.ResetareParolaActivity" />
        <activity android:name=".profesor.AlegereContactActivityProfesor" />
        <activity android:name=".logare.SignUpActivity" />
        <activity android:name=".logare.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>